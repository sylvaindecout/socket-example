<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>socket-example</artifactId>
        <groupId>test.sdc.socket</groupId>
        <version>01.00.00-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>protobuf-model</artifactId>

    <properties>
        <!-- Exclude "protocol" data model classes, as they are automatically generated by Protobuf. -->
        <sonar.exclusions>/src/main/java/test/sdc/socket/model/protocol/**/*</sonar.exclusions>

        <!-- Maven plug-in versions. -->
        <protoc-maven-plugin.version>0.3.6</protoc-maven-plugin.version>
    </properties>

    <dependencies>
        <!-- Protobuf -->
        <dependency>
            <groupId>com.google.protobuf</groupId>
            <artifactId>protobuf-java</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Protobuf plugin, used to generate classes from proto files -->
            <!-- This requires protobuf to be installed (https://github.com/google/protobuf/releases) and accessible in PATH of the server that builds the project. -->
            <plugin>
                <groupId>net.dongliu</groupId>
                <artifactId>protoc-maven-plugin</artifactId>
                <version>${protoc-maven-plugin.version}</version>
                <configuration>
                    <outputDirectory>${project.build.sourceDirectory}</outputDirectory>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>


</project>